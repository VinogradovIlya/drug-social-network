{
  "build": {
    "command": "pip install --no-cache-dir -r requirements.txt"
  },
  "start": {
    "command": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --log-level error"
  }
}